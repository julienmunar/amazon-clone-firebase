/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.cdnlogo.com','images-na.ssl-images-amazon.com'],
  },
}

module.exports = nextConfig
